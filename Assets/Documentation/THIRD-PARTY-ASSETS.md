# 第三方素材说明

本初版没有分发外部模型、图片、音乐或音效。

按照项目约束，开始制作场景前已检索全局 `ange-embed` v2 素材库，检查了 `h5_respack_v1` 的标签并搜索 `chinese_style` 墓穴、建筑和道具；本次没有返回同时适合固定墓室布局且可直接确认授权元数据的命中，因此场景暂用 Unity Primitive 和程序化材质。后续替换正式资产时，应保留作者、许可和来源 URL。
